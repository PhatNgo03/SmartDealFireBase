package com.example.smartdealfirebase.DesignPatternFactoryMethod;

// Tạo Interface để xác định hành động cho đối tượng đăng kí => Product
public interface IRegistration {
    void register(); // Thực hiện việc đăng kí
}
